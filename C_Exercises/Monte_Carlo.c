#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int pi, x, y;

    //do some stuff

    rand()=x; // randomly generate coordinates for (x, y)
    rand()=y;

        if (x<=r && y<=r)

            


    pi=(

    printf("PI=%d", pi);



return 0;
}